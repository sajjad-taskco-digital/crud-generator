<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use {{ modelNamespace }}\{{ model }};

class {{ class }} extends Seeder
{
    public function run(): void
    {
        for ($i = 0; $i < 5; $i++) {
            {{ model }}::create([
                'title'       => 'Sample {{ model }} ' . ($i + 1),
                'description' => 'This is a sample {{ model }} description.',
            ]);
        }
    }
}
